export default function JaggedSection({ children }) {
    return (
        <section className=" jagged after:bg-papaya-whip-100 before:bg-papaya-whip-100 w-full">
            {children}
        </section>
    );
}
